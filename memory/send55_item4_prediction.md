# SEND-55 · ITEM 4 PREDICTION — WRITTEN BEFORE IMPLEMENTATION

Recorded 2026-08-19, BEFORE the correction metric was implemented and
BEFORE Howard's next field run. These are the numbers the metric is
expected to report; if the measured numbers disagree, the disagreement
is the finding.

## Predictions (Howard, SEND-55 authorization, verbatim intent)
1. **Vertical error ≈ 0** — proposal boxes are datum-bracketed
   (FIRST FLOOR → topmost TOP OF PLATE, per the bottom-edge ruling:
   both the derived band and the proposal stay FF → plate).
2. **Horizontal error ≈ 10–16 ft too wide** — the registered
   leader-line offset (labels sit at the end of leader lines outside
   the wall; inner-edge census residuals were +10.1 to +15.7 ft).

## Expected event mix on Letrick (post Item-2 ladder)
- front / left / right: CORRECTED (derived-chain tier) — mostly
  horizontal pull-in of left and right edges.
- rear: CORRECTED (contested tier, larger contestant named) — same
  horizontal pull-in expected; vertical near zero.
- Gables: ADDED FROM SCRATCH (no gable proposals exist — line-work
  read not built).

Nothing below this line may be edited after the metric lands.
