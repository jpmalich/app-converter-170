# SEND-69 — LINE-WORK READ PREDICTION (written BEFORE the read runs; unrevisable)

Recorded 2026-08-19, before wiring is executed against either house.
Scoring appends below the line; nothing above it may be edited.

1. **Width residual falls to near zero where the read RESOLVES.** The
   measured field residual was 10.1–13.7 ft (Howard: ~15) — exactly the
   leader offset the datum-span geometry carries. If line-work resolves
   and the residual against Howard's sealed widths (54'-0"/30'-0"
   Letrick, 58'-0"/30'-2"/33'-0" Boni) is anything OTHER than near
   zero, it is a different error, not a smaller version of this one.
2. **Letrick REAR shows the 2'-7" chimney.** Howard sealed it. A rear
   polygon that comes back a plain 4-vertex rectangle is finding the
   wall but NOT the projection — reading geometry vs re-deriving a box.
   Prediction: rear resolves with MORE THAN FOUR vertices, or refuses.
3. **Boni single-corner FRONT and REAR become reachable** — line-work
   needs no corner labels, so the permanent single-corner ladder limit
   does not apply to it (the design's own claim). Boni RIGHT stays at
   risk of INDETERMINATE (no FIRST FLOOR datum located — the lower
   anchor is missing; the read must refuse, not improvise).
4. **Vertex counts are the evidence of a real read.** >4 vertices on
   any face means steps were found. All-rectangles-everywhere on a
   house Howard zoned as 3 rear pieces would be suspicious, not clean.
5. **The sheet border is excluded STRUCTURALLY** (band containment: a
   qualifying stroke lies entirely inside the face's title-carved band;
   the border always exits the band on at least one end since it spans
   the page across bands and the title block). If a face comes back at
   roughly page width, the exclusion failed and it must be reported as
   the pre-Ruling-ZZ shape reappearing, never as progress.
6. **A refused read keeps the datum span under ITS OWN geometry tier**
   ("datum_span_after_linework_refused") with the basis sentence naming
   the leader offset — a good run and a fallback run must be
   distinguishable on screen.

No parameter in the read is fit to 54, 30, 58, 30'-2" or 33 — those are
checks, never targets. Purity pin holds.

---
(scoring appends below — do not edit above)
